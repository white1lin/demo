# 从这里开始

这个项目的正确使用方式不是“直接复制代码”，而是把它当成一次 AI 产品训练。

## 第 0 步：先建立产品意识

先读：

- `PRODUCT.md`
- `LEARNING_GUIDE.md`

你要能回答：

- 用户是谁？
- 他为什么需要这个工具？
- 输入是什么？
- 输出是什么？
- 为什么第一版不做登录、付费、爬虫？

## 第 1 步：先跑假数据版本

不要急着配置 API key。先运行项目，点击“生成匹配分析”，观察模拟结果。

你要理解：

- 页面如何收集岗位和简历。
- loading、错误、空状态为什么重要。
- 为什么历史记录先用浏览器保存。

问 Codex：

```text
请带我读 src/app/page.tsx。每解释一个概念，都告诉我它在产品体验中解决什么问题。
```

## 第 2 步：理解服务端 API

读：

- `src/app/api/analyze-job/route.ts`
- `src/app/api/match-resume/route.ts`
- `src/lib/ai.ts`

你要理解：

- 前端不能直接放 API key。
- API route 是浏览器和模型之间的中间层。
- prompt、schema、错误处理都是产品质量的一部分。

问 Codex：

```text
请解释这个项目的前端、API route、OpenAI API 之间的数据流，用文字图画出来。
```

## 第 3 步：接真实模型

复制 `.env.example` 为 `.env.local`，填入：

```bash
OPENAI_API_KEY=你的 key
OPENAI_MODEL=gpt-4.1-mini
```

重新运行项目，再用真实岗位测试。

你要观察：

- 输出是否具体。
- 有没有胡说。
- JSON 结构是否稳定。
- 建议是否真的帮你改简历。

## 第 4 步：做第一个自己的改动

建议你做一个很小的功能：增加“投递行动清单”。

问 Codex：

```text
请给这个产品增加“投递行动清单”：输出今天要做的 3 个动作。先解释需要改哪些文件，再实施。
```

你要重点看 Codex 会改：

- 类型定义。
- AI schema。
- prompt。
- 页面展示。

这就是 AI 产品开发的典型链路。

## 第 5 步：用真实数据复盘

找 3 个岗位，用同一份简历测试。记录：

- 哪些建议有用。
- 哪些建议空泛。
- 哪些字段你根本不看。
- 你最想加的下一个功能是什么。

然后问 Codex：

```text
我测试了 3 个岗位，下面是结果和我的感受。请帮我做产品复盘，并决定下一版优先级。
```

## 学习原则

- 每次只让 Codex 做一个小任务。
- 每次改完都让 Codex 解释为什么。
- 不懂就问，不要跳过。
- 先做可用闭环，再加复杂功能。
- 你是在训练产品判断，不是在背代码。
