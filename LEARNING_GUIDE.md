# 学习路线：从 0 用 Codex 做 AI 产品

## 第 1 阶段：理解 AI 产品

AI 产品不是“聊天框 + 模型”。一个好 AI 产品会把用户输入变成稳定、有用、可重复的输出。

你要理解三个层次：

- 普通软件：按钮、表单、列表、保存。
- AI 能力：理解文本、提取信息、生成建议。
- 产品工作流：把 AI 能力嵌入一个明确任务。

本项目的任务是：帮助你判断一个岗位是否值得投。

## 第 2 阶段：定义 MVP

不要一开始做“大而全求职平台”。第一版只做一个完整闭环：

```text
岗位文本 + 简历文本 -> AI 分析 -> 可保存的结果
```

判断 MVP 是否成立，只看一个问题：它能不能帮你更快决定“投不投、怎么改简历”。

## 第 3 阶段：先做假数据

先不要急着接 OpenAI API。先用模拟结果跑通页面，这会帮你理解产品体验：

- 输入框是否够用。
- 结果是否容易阅读。
- 历史记录是否有价值。
- 错误状态是否清楚。

这一步学的是产品，不是模型。

## 第 4 阶段：再接真实 AI

接入模型时，你要重点学三件事：

- prompt 要描述角色、任务、输入和输出格式。
- 输出最好是 JSON，方便页面稳定展示。
- 模型可能失败，所以要有错误提示和模拟兜底。

## 第 5 阶段：指挥 Codex

好的 Codex 指令应该小而清楚：

- “先帮我解释这个文件负责什么。”
- “帮我只修改 API，不要动页面。”
- “给这个功能加一个失败状态，并解释为什么要加。”
- “用初学者能懂的方式解释这段 TypeScript。”

你要把 Codex 当成工程教练，而不是许愿机。

## 第 6 阶段：验证和复盘

用 3 个真实岗位和 1 份简历测试：

- 岗位摘要是否准确。
- 匹配分是否符合直觉。
- 简历建议是否具体。
- 面试准备点是否有帮助。

复盘后再决定第二版做什么。
