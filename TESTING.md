# 测试清单

AI 输出质量的人工评测场景和复盘表见 [EVALUATION.md](./EVALUATION.md)。

## 产品测试

- 粘贴一个中文岗位，能生成岗位摘要。
- 粘贴一个英文岗位，能生成岗位摘要。
- 粘贴简历后，能生成匹配分和修改建议。
- 不填 OpenAI key 时，能看到模拟结果。
- 输入为空时，页面提示补充内容。
- 超过 12,000 个字符时，页面提示删减内容，服务端也会拒绝请求。
- 模型服务失败时，页面显示可恢复错误，不暴露 Key 或底层请求内容。
- 分析完成后，结果会进入历史记录。
- 刷新页面后，历史记录仍然存在。
- 清除历史后，页面不再显示历史记录；刷新后也不会恢复。

## 学习验收

你应该能解释：

- 为什么先做 MVP，而不是完整平台。
- 为什么先做假数据，再接真实 AI。
- 为什么 API key 放在服务端。
- 为什么要求 AI 返回 JSON。
- 为什么第一版用 localStorage 保存历史。
